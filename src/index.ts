// import { User } from './User';
// import { Company } from './Company';

// we need the reference to an html element to place the map

new google.maps.Map(document.getElementById('map'), {
  center: {
    lat: 0,
    lng: 0
  },
  zoom: 1
});
